# Current Plan for Local AI

Full plan file:

`docs/PLC_SKILL_MCP_NEXT_DEVELOPMENT_PLAN.md`

Start with Phase 1:

1. Add MCP contract tests.
2. Compare `tools/mcp_server/schemas.py::TOOL_DEFINITIONS` with `tools/mcp_server/toolchain.py::TOOLS`.
3. Fix any mismatch before changing behavior.
4. Then move to Phase 2 and sync Skill / README / Roadmap documents.

Do not prioritize `PrintDemo` business logic. Focus on Skill, MCP Server, policy, config, docs, and tests.
